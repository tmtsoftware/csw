include required("logging.conf")
include required("test-serialization.conf")

csw-networks.hostname.automatic = on
csw-alarm {
  refresh-interval = 1s
  max-missed-refresh-counts = 1
  shelve-timeout = "8:00:00 AM"
}