csw-database.hikari-datasource.dataSource {
  serverName = localhost
  //portNumber = randomPort set programmatically from embedded postgres
  databaseName = postgres
  user = postgres
  password = postgres
}