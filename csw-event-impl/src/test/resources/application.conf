include "logging.conf"

akka {
  loggers = [csw.services.logging.compat.AkkaLogger]
  loglevel = debug
  actor {
    debug {
      lifecycle = off
    }
  }
}

csw-logging {
  akkaLogLevel = debug
  slf4jLogLevel = debug
  logLevel= debug
}