include "logging.conf"
include "test-serialization.conf"

csw-logging {
  logLevel = debug
  appender-config {
    stdout {
      oneLine = true
    }
  }
}

auth-config {
  client-id = tmt-backend-app
}
