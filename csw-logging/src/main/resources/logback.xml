<configuration scan="false">

    <appender name="svc_appender" class="csw.services.logging.Slf4jAppender">
    </appender>

    <root level="${SLF4J_LEVEL:-WARN}">
        <appender-ref ref="svc_appender"/>
    </root>

</configuration>
